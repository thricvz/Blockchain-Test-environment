FROM ubuntu:jammy

# Install software dependencies

RUN apt update && apt upgrade -y
RUN apt install g++ net-tools cmake git gdb -y

WORKDIR /
RUN git clone -b P2P https://github.com/thricvz/Blockchain-From-Scratch.git src/

RUN mkdir build 
RUN cmake -S src -B build
RUN cmake --build build
